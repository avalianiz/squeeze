# Pinned upstream versions for reproducible Squeeze sidecar builds.
# Bump these together and rebuild all target triples via CI.
FFMPEG_VERSION=7.1.1
X264_VERSION=stable
# Optional: set to a commit SHA of videolan/x264 for stricter pins.
# X264_COMMIT=
